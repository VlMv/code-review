/* eslint-disable no-unused-vars */
import { mockCommits } from '../../../mock/commit';

const fetchCommitsForTimePeriod = (
  user,
  token,
  repository,
  sinceDate,
  untilDate
) => mockCommits;


export default fetchCommitsForTimePeriod;

